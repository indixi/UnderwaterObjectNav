# Install a CUDA-enabled PyTorch build separately from https://pytorch.org/get-started/locally/.
# Do not install openmim in this environment: its current OpenDataLab dependency
# pins setuptools~=60.2.0, conflicting with current PyTorch (setuptools>=77.0.3).
mmengine>=0.10.0,<1.0.0
mmdet>=3.3.0,<4.0.0
pycocotools>=2.0.7
Pillow>=10.0
